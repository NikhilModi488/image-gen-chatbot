version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:7860"
    environment:
      - PORT=7860
      - OLLAMA_URL=http://localhost:11434
      - USE_OLLAMA_PROMPT_REFINEMENT=True
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
